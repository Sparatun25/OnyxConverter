[build]
  command = "node build.js"
  publish = "."

[build.environment]
  NODE_VERSION = "20"

# Переменные аналитики задаются в настройках сайта Netlify (Site settings → Environment),
# чтобы не хранить их в коде:
#   PLAUSIBLE_DOMAIN, GA_ID, GSC_VERIFY, SITE_URL
